# 2023-09-20    12:51
=====================


Rollback
--------

# откат изменений в каком-то конкретном файле
    $ git checkout <filename>   

# проверка изменений в каком-то файле
    $ vim sun
какие-то изменения
    $ git diff sun
diff --git a/sun b/sun
index e69de29..4f25d05 100644
--- a/sun
+++ b/sun
@@ -0,0 +1 @@
+какие-то изменения

# после кэширования изменений перед коммитом [git add .] потребуется указывать флаг '--cached'


# для отката изменений после кэширования
    $ git restore --staged <filename>   
    
# для проверки изменений в каком-то коммите с текущим состоянием
    $ git diff <commit>

# возврат состояния к предыдущему коммиту, будет создан новый коммит
    $ git revert HEAD

# возврат состояник с заданному предудыщему коммиту без сохранения истории изменений в новом коммите
    $ git reset --hard <commit>

# не трогает индексный файл или рабочее дерево (но сбрасывает заголовок в <commit>, как и все режимы). Это оставляет все ваши измененные файлы «Изменения, подлежащие фиксации», как это указано в статусе git.    
    $ git reset --soft <commit>
    
